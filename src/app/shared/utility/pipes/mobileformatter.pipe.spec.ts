import { MobileformatterPipe } from './mobileformatter.pipe';

describe('MobileformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
