import { RoutePermissionDirective } from './route-permission.directive';

describe('RoutePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
